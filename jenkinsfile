pipeline {
    agent any

    environment {
        IMAGE_REPO = 'ghcr.io/ashmit-gupta/k8-test-project'
    }

    stages {

        stage('Checkout') {
            steps {
                git branch: 'main',
                    url: 'https://github.com/Ashmit-Gupta/k8-test-project.git'

                script {
                    // Immutable tag = short git SHA (ties image to exact commit)
                    env.IMAGE_TAG  = sh(returnStdout: true, script: 'git rev-parse --short=7 HEAD').trim()
                    env.FULL_IMAGE = "${env.IMAGE_REPO}:${env.IMAGE_TAG}"
                    echo "Image: ${env.FULL_IMAGE}"
                }
            }
        }

        stage('Build Docker Image') {
            steps {
                sh '''
                    docker build \
                        -f dockerfile \
                        -t $FULL_IMAGE \
                        -t $IMAGE_REPO:latest \
                        .
                '''
            }
        }

        stage('Login to GHCR') {
            steps {
                withCredentials([
                    usernamePassword(
                        credentialsId: 'github-token',
                        usernameVariable: 'GITHUB_USER',
                        passwordVariable: 'GITHUB_TOKEN'
                    )
                ]) {
                    sh '''
                        echo "$GITHUB_TOKEN" | docker login ghcr.io \
                            -u "$GITHUB_USER" \
                            --password-stdin
                    '''
                }
            }
        }

        stage('Push Image') {
            steps {
                sh '''
                    docker push $FULL_IMAGE
                    docker push $IMAGE_REPO:latest
                '''
            }
        }

        stage('Deploy to Kubernetes') {
            steps {
                withCredentials([
                    file(credentialsId: 'kubeconfig', variable: 'KUBECONFIG_FILE')
                ]) {
                    sh '''
                        export KUBECONFIG=$KUBECONFIG_FILE

                        kubectl get nodes

                        # Leave manifests in Git unchanged (image: ...:latest)
                        kubectl apply -f k8s/namespace.yml
                        kubectl apply -f k8s/service.yml
                        kubectl apply -f k8s/ingress.yml
                        kubectl apply -f k8s/deployment.yml

                        # Pin live Deployment to this commit's image tag
                        kubectl set image deployment/hello-app \
                            hello-app=$FULL_IMAGE \
                            -n k8s-demo-namespace

                        kubectl rollout status deployment/hello-app \
                            -n k8s-demo-namespace \
                            --timeout=120s

                        kubectl get pods -n k8s-demo-namespace -o wide
                    '''
                }
            }
        }
    }

    post {
        always {
            sh 'docker logout ghcr.io || true'
        }

        success {
            echo "Pipeline OK — deployed ${FULL_IMAGE}"
        }

        failure {
            echo 'Pipeline failed!'
        }
    }
}
